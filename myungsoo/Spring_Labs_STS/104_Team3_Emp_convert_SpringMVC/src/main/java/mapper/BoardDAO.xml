<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.EmpDaoI">
 <!-- insertBoard(글 입력하기) -->
 <select id="getEmpAllList" parameterType="dao.EmpDaoI" resultType="dto.EmpDto">
   select empno,ename,job,nvl(mgr,0) as mgr,hiredate,sal,nvl(comm,0) as comm, deptno from emp order by empno
 </select>
 
 
 <insert id="insertEmp">
 <!-- jdbcType=VARCHAR -->
 	insert into emp(empno, ename, job, mgr,hiredate,sal,comm,deptno) 
 	values(#{empno},#{ename},#{job},${mgr},sysdate,${sal},${comm},${deptno})
 </insert>
 
 <delete id="deletEmp">
 	delete emp where empno= #{empno}
 </delete>
</mapper>